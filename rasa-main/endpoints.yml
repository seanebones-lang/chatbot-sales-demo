# This file contains the different endpoints your bot can use.
# https://rasa.com/docs/rasa/messaging-and-customizing/action-server/

# This is the server that runs the custom actions.
action_endpoint:
  url: "http://localhost:5055/webhook"

# This is the server that runs the custom actions.
# action_endpoint:
#   url: "http://localhost:5055/webhook"
#  password: <password used for authentication>
#  login_db: <database name associated with the user's credentials>
