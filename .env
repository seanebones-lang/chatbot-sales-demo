# Recipient for incoming demo leads
OWNER_EMAIL=seanebones@icloud.com

# Optional: set a friendly From address (if allowed by your SMTP)
# FROM_EMAIL=demo@yourdomain.com

# Configure one of the following to enable real email delivery:
# iCloud SMTP (requires an app-specific password)
# SMTP_HOST=smtp.mail.me.com
# SMTP_PORT=587
# SMTP_USER=seanebones@icloud.com
# SMTP_PASS=YOUR_APP_SPECIFIC_PASSWORD

# Or use another SMTP provider (Gmail, SendGrid, Mailgun, etc.)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=YOUR_SENDGRID_API_KEY

# For testing only (no real delivery). If set to true, ignores SMTP above.
# ETHEREAL=true
