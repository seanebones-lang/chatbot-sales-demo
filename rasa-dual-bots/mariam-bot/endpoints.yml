# This file contains the different endpoints your bot can use.
# https://rasa.com/docs/rasa/messaging-and-custom-channels

# This is the server that runs your custom actions.
# https://rasa.com/docs/rasa/custom-actions
action_endpoint:
  url: "http://localhost:5058/webhook"

# This is the server that runs your bot.
# https://rasa.com/docs/rasa/deployment
server:
  url: "http://localhost:5005"
  token: "mariam_token_2024"
  wait_time_between_pulls: 10

# This is the server that runs your tracker store.
# https://rasa.com/docs/rasa/tracker-stores
# Using in-memory tracker store for local development
# tracker_store:
#   type: "redis"
#   url: "localhost"
#   port: 6379
#   db: 0
#   password: ""
#   use_ssl: false

# Simplified endpoints for local development
# Remove complex configurations that might cause startup issues
