# This file contains the different endpoints your bot can use.
# https://rasa.com/docs/rasa/messaging-and-voice-channels

# Server where the models are pulled from.
# https://rasa.com/docs/rasa/model-storage#fetching-models-from-a-server

models:
  # url: http://my-server.com/models/default_core@latest
  # wait_time_between_pulls:  10   # [optional](default 100)

# Server which runs your custom actions.
# https://rasa.com/docs/rasa/custom-actions

action_endpoint:
  url: "http://localhost:5055/webhook"

# Tracker store which is used to store the conversations.
# By default the conversations are stored in memory.
# https://rasa.com/docs/rasa/tracker-stores

#tracker_store:
#  type: redis
#  url: <host of the redis instance, e.g. localhost>
#  port: <port of your redis instance, usually 6379>
#  db: <number of your database within redis, e.g. 0>
#  password: <password used for authentication>
#  use_ssl: <whether or not to communicate via SSL, e.g. false>
#  client_name: <name of the client, e.g. botname>

#tracker_store:
#  type: mongod
#  url: <url to your mongo instance, e.g. mongodb://localhost:27017>
#  db: <name of the db within your mongo instance, e.g. rasa>
#  username: <username used for authentication>
#  password: <password used for authentication>
#  auth_source: <database name associated with the user's credentials>

#tracker_store:
#  type: postgresql
#  url: <host of the postgres instance, e.g. localhost>
#  port: <port of your postgres instance, e.g. 5432>
#  db: <name of the db within your postgres instance, e.g. rasa>
#  username: <username used for authentication>
#  password: <password used for authentication>
#  login_db: <database name associated with the user's credentials>

# Event broker which all conversation events stream to.
# https://rasa.com/docs/rasa/event-brokers

#event_broker:
#  url: localhost
#  username: username
#  password: password
#  queue: queue

#event_broker:
#  type: pika
#  url: localhost
#  username: username
#  password: password
#  queue: queue

#event_broker:
#  type: kafka
#  url: localhost
#  username: username
#  password: password
#  topic: topic

#event_broker:
#  type: sql
#  dialect: "postgresql"  # or "mysql", "sqlite", "oracle"
#  url: localhost
#  username: username
#  password: password
#  db: db_name
#  query: "SELECT * FROM table_name"

#event_broker:
#  type: redis
#  url: localhost
#  port: 6379
#  db: 0
#  password: password
#  use_ssl: false
#  channel: rasa_core_events

# Lock store which is used to store the conversation locks.
# By default the locks are stored in memory.
# https://rasa.com/docs/rasa/lock-stores

#lock_store:
#  type: redis
#  url: <host of the redis instance, e.g. localhost>
#  port: <port of your redis instance, usually 6379>
#  db: <number of your database within redis, e.g. 0>
#  password: <password used for authentication>
#  use_ssl: <whether or not to communicate via SSL, e.g. false>
#  client_name: <name of the client, e.g. botname>

#lock_store:
#  type: "postgresql"
#  url: <host of the postgres instance, e.g. localhost>
#  port: <port of your postgres instance, e.g. 5432>
#  db: <name of the db within your postgres instance, e.g. rasa>
#  username: <username used for authentication>
#  password: <password used for authentication>
#  login_db: <database name associated with the user's credentials>
